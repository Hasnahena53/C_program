//cube of a given number
#include<stdio.h>
int tori (int x);
int main()
{
    int a,x;
    printf("enter the number:");
    scanf("%d",&a);
    x=a*a*a;
    int tori (x);
    printf("%d",x);
}
int tori (int x)
{
    return(x);
}
